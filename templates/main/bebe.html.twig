{% extends 'base.html.twig' %}

{% block title %}Liste des produits bebe
{% endblock %}

{% block body %}
   
	<h1 class="text-center my-3">Liste des produits</h1>

	<div class="row">

		{% for bebe in bebe %}

			<div class="col-md-4">
				<div class="card mb-3" style="width: 18rem;">
					<h3 class="card-header">{{ bebe.titre }}</h3> 
					<img src="{{ bebe.photo }}" alt="Image du produit">
					<div class="card-body">
						<p class="card-text text-end fw-bold">{{ bebe.prix / 100|number_format(2)}}€</p>
					</div>
					<h6 class="card-header">{{ bebe.description }}</h6> 
					<div class="card-body text-end">
                    <a href="{{path("cart_add",{id:bebe.id})}}" class="card-link btn btn-primary">Ajouter au panier</a>
                  </div>
				</div>
			</div>

		{% endfor %}

	</div>

{% endblock %}
